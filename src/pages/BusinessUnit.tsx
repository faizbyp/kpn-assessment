import { Typography } from "@mui/material";

const BusinessUnit = () => {
  return (
    <Typography variant="h1" color="primary">
      Business Unit
    </Typography>
  );
};
export default BusinessUnit;
